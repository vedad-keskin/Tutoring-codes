#include <iostream>
using namespace std;


//struct Struktura {
//private:
//	int broj;
//
//
//};
//
//class Klasa {
//public:
//	int broj;
//
//
//
//};


// POKAZIVACI

// 1. ALOKACIJA ( new ) 

// ( ) --> jedno mem. mjesto

// [ ] --> vise mem. mjesta 

// 2. DEREFERENCIRANJE ( * ) 

// ( ) --> jedno mem. mjesto --> mora deref. *_ECTS = 0 , _ECTS = 00000198BCDD8200

// [ ] --> vise mem. mjesta --> ne smije deref. *_naziv = N , _naziv = Nema predmeta

// 3. DESTRUKTOR / DEALOAKCIJA ( delete , nullptr ) 

// ( ) --> jedno mem. mjesto --> delete _ECTS; 

// [ ] --> vise mem. mjesta -->  delete[] _naziv;



class Predmet {
public:			  // dft. constr.  //
	char* _naziv; // Nema predmeta // nullptr
	//char _naziv[50];

	int _ocjena;  //     5		   //
	int* _ECTS;	  //     0 		   //


	// 1. dft. const

	Predmet() {

		_ocjena = 5;

		_ECTS = new int(0);

		//char dftNaziv[] = "Nema predmeta"; // 14 
		//string dftNaziv = "Nema predmeta";
		const char* dftNaziv = "Nema predmeta"; // string literal -> read only mem. 

		int vel = strlen(dftNaziv) + 1; // 13 + 1 = 14 

		_naziv = new char[vel];

		strcpy_s(_naziv, vel, dftNaziv);


	}

	// 4. user-def constr.
	//Predmet predmet1("Programiranje II", 9, 7); // <-- user-def constr.

	Predmet(const char* naziv , int ocjena, int ects) {

		_ocjena = ocjena;

		_ECTS = new int(ects);

		int vel = strlen(naziv) + 1;

		_naziv = new char[vel];

		strcpy_s(_naziv, vel, naziv);


	}

	// 5. copy constr. 
	//Predmet predmetKopija2(predmet2);  // shallow copy --> deep copy --> copy constr.

	// bez & -> pravimo kopiju
	// sa & -> saljemo original
	// sa const + & -> stitimo original

	Predmet(const Predmet& predmet) { // napravi kopija tog predmet1 -> posalji original 

		//predmet._ocjena = 10;

		_ocjena = predmet._ocjena;

		_ECTS = new int(*predmet._ECTS);

		int vel = strlen(predmet._naziv) + 1;

		_naziv = new char[vel];

		strcpy_s(_naziv, vel, predmet._naziv);

	}

	// 6. op = 
	//predmetOperatorDodjele = predmet1;  // shallow copy --> deep copy --> operator = 

	Predmet& operator = (const Predmet& predmet) {

		if (this != &predmet) {

			delete _ECTS;

			delete[] _naziv;



			_ocjena = predmet._ocjena;

			_ECTS = new int(*predmet._ECTS);

			int vel = strlen(predmet._naziv) + 1;

			_naziv = new char[vel];

			strcpy_s(_naziv, vel, predmet._naziv);

		}





		// this -> adresa tog glavnog objekta
		// *this -> ref. glavni obj.

		return *this;

	}



	 

	// 3. desturktor / dealokacija 

	// TILDA -> alt gr + 1 -> ~~~~~~

	~Predmet() {

		cout << "DESTUKTOR SE POZVAO" << endl;

		delete _ECTS; 

		_ECTS = nullptr;

		delete[] _naziv; 

		_naziv = nullptr;

	}





	// 2. ispis

	void Ispis() {

		cout << "Naziv predmeta: " << _naziv << endl;
		cout << "Ocjena predmeta: " << _ocjena << endl;
		cout << "ECTS predmeta: " << *_ECTS << endl;

	}



};



class Student {
	char* _imePrezime;
	Predmet* _polozeni;
	int _trenutno;
	int _max;
public:
	// Kreirati i implementirati
	// 1. Defaultni konstruktor (default constructor)
	// 2. Konstruktor koji prima parametre (user-defined constructor)
	// 3. Konstruktor kopije (copy constructor)
	// 4. Operator dodjele (operator =)
	// 5. Dealokacija (destructor)
	// 6. Ispis ili operator ispisa (operator <<) 
	// 7. Dodatne funkcije



	void AddPolozeniPredmet(Predmet predmet) {
		// Dodaje novi polozeni predmet u niz
		// Voditi računa da se ne može prekoračiti kapacitet niza
		// Voditi računa da se ne moze dodavati isti predmet (istog naziva)		        


	}

};



void main() {

	Predmet predmet0; // <-- dft. constr.

	Predmet predmet1("Programiranje II", 9, 7); // <-- user-def constr.
	Predmet predmet2("Matematika II", 6, 4); // <-- user-def constr.

	Predmet predmetKopija1 = predmet1; // shallow copy --> deep copy --> copy constr.
	Predmet predmetKopija2(predmet2);  // shallow copy --> deep copy --> copy constr.


	Predmet predmetOperatorDodjele; // <-- dft. constr.

	predmetOperatorDodjele = predmetOperatorDodjele;  // shallow copy --> deep copy --> operator = 




	predmet0.Ispis(); // <-- ispis
	cout << endl;
	predmet1.Ispis(); // <-- ispis
	cout << endl;
	predmet2.Ispis(); // <-- ispis
	cout << endl;
	predmetKopija1.Ispis(); // <-- ispis
	cout << endl;
	predmetKopija2.Ispis(); // <-- ispis
	cout << endl;
	predmetOperatorDodjele.Ispis(); // <-- ispis
	cout << endl;

	//Struktura s;
	//Klasa k;

	//s.broj;
	//k.broj;

	system("pause>0");
}

